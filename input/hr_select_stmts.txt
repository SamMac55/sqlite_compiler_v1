// basic select
employees all;
employees first_name;

// select with order clause
employees first_name,last_name,salary sorted by salary desc;

// join and where clauses
employees first_name, last_name with employee_projects emp_role using employees.employee_id with project_id at most 2;

//multiple joins
employees first_name,last_name with employee_projects emp_role using employees.employee_id 
with projects project_name using employee_projects.project_id;

// where clause again
projects project_name with budget at most 45000;

// limit clause
find 1 employees first_name,last_name,salary sorted by salary with department_id greater than 1;

//count * with attribute list
employees department_id with departments count all, dept_name using employees.department_id group by employees.department_id,dept_name;