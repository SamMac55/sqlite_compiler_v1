// create table
new table test_table {
int id(pk),
String name
};

// insert
add employees (employee_id=900,first_name='John',last_name='Doe',email='john@example.com',salary=50000,department_id=1);

// insert multiple
add test_table (id=1, name='testing'), (id=2,name='testing2');

// delete row
remove employees with employee_id is 1;

// see if the new table was added properly
test_table all;

// drop table
remove table test_table;

// see if our new employee was added
employees all with employee_id is 900;

remove employees with employee_id is 900;